# 00-minimal

A very small configuration intended to be easy to understand.

- Area: `health`
- Actions:
  - `walk` (button)
  - `stretch` (checkbox)

Try it with a block like:

```progress-tracker
{ "date": "2026-01-01", "activitiesLayout": "list" }
```
