# 03-number-actions

Demonstrates `number`-input actions.

- `deep_work_minutes` is a typed numeric input.
- The example uses both `input.max` and per-action `max`.
  - The planning and logging UIs clamp values to the effective maximum.

This is useful for actions like “minutes practiced”, “pages read”, or “push-ups”.
